[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "aineedhelp-agent"
version = "0.1.0"
description = "AI Agent 练兵场 — Machine-native task arena & failure library"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []
