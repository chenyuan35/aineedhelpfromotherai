import { cpSync, mkdirSync, existsSync, rmSync, readFileSync, writeFileSync, readdirSync, statSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';
import { execSync } from 'child_process';

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, '..');
const dist = join(root, 'dist');
const homePath = join(root, 'index.html');
const curatedHome = readFileSync(homePath, 'utf8');
const GA4_ID = 'G-FYKKNKRE58';
const GA4_TAG = `<script async src="https://www.googletagmanager.com/gtag/js?id=${GA4_ID}"></script><script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments)}gtag('js',new Date());gtag('config','${GA4_ID}');</script>`;
const BEHAVIOR_TAG = `<script id="site-behavior-analytics">(()=>{const send=(name,params)=>{try{if(typeof window.gtag==='function')window.gtag('event',name,params)}catch{}};const placement=el=>el.closest('.continuity-actions')?'home_hero_action':el.closest('.continuity-map')?'home_job_map':el.closest('.provider-stage')?'home_provider':el.closest('.tool-search-results')?'site_search':el.closest('.related-grid')?'related_tool':el.closest('.tools-grid')?'tool_listing':el.closest('.utility-row')?'home_utility':el.closest('.relay-wrap')?'home_relay':el.closest('footer')?'footer':'internal';document.addEventListener('click',event=>{const link=event.target.closest('a[href]');if(link){const sourcePath=location.pathname;try{const raw=link.getAttribute('href')||'';if(link.closest('.continuity-hero')){const job=raw.includes('#ai-reset-tools')?'reset':raw.includes('/phone-number-survival-guide/')?'access':raw.includes('/relay-exit-risk-checker/')?'reliability':'';if(job)send('home_job_select',{job,source_path:sourcePath,placement:placement(link)})}const url=new URL(link.href,location.href);if(url.origin===location.origin&&url.pathname.startsWith('/tools/'))send('tool_open',{tool_path:url.pathname,source_path:sourcePath,placement:placement(link)})}catch{}}const button=event.target.closest('main button');if(button)send('tool_action',{source_path:location.pathname,action_id:(button.id||button.textContent||'button').trim().slice(0,80)})})})();</script>`;

const sourceSnapshot = new Map();
const snapshotSkip = new Set(['dist','node_modules']);
function snapshotSources(dir){
  for(const name of readdirSync(dir)){
    if(snapshotSkip.has(name))continue;
    const path=join(dir,name);const st=statSync(path);
    if(st.isDirectory())snapshotSources(path);else if(st.isFile())sourceSnapshot.set(path,readFileSync(path));
  }
}
function restoreSources(dir=root){
  for(const name of readdirSync(dir)){
    if(snapshotSkip.has(name))continue;
    const path=join(dir,name);const st=statSync(path);
    if(st.isDirectory())restoreSources(path);else if(st.isFile()&&!sourceSnapshot.has(path))rmSync(path,{force:true});
  }
  if(dir===root){for(const [path,content] of sourceSnapshot){mkdirSync(dirname(path),{recursive:true});writeFileSync(path,content)}}
}
snapshotSources(root);
for(const path of [join(root,'..','ai.txt'),join(root,'..','llms.txt')]){if(existsSync(path))sourceSnapshot.set(path,readFileSync(path));}
try {
rmSync(dist,{recursive:true,force:true});
mkdirSync(dist,{recursive:true});

for (const script of ['generate-site-pages.mjs','generate-tools.mjs','generate-sitemap.mjs','generate-ai-reset-tools.mjs','fix-ai-reset-output.mjs','generate-ai-reset-tools-round2.mjs','enhance-ai-credit-burn.mjs','enhance-bolt-reset.mjs','enhance-manus-reset.mjs','enhance-replit-reset.mjs','enhance-cursor-breakthrough.mjs','refine-home-ux.mjs','register-relay-risk-checker.mjs','register-claude-reset.mjs','register-tiktok-publish.mjs','register-phone-survival-guide.mjs','apply-theme.mjs','fix-cross-site-visual-tokens.mjs','normalize-freshness-metadata.mjs']) {
  execSync(`node bin/${script}`,{cwd:root,stdio:'inherit'});
}
execSync('node bin/check-utility-regressions.mjs',{cwd:root,stdio:'inherit'});
// The homepage is intentionally hand-curated. Generators may update shared pages and
// indexes during the build, but they must not replace the checked-in homepage identity.
const claudeSoon = '<span class="provider-link provider-soon" aria-label="Claude tool planned"><b>CL</b><span><strong>Claude</strong><small>soon</small></span></span>';
const claudeLive = '<a class="provider-link" href="/tools/claude-code-limit-reset/"><b>CL</b><span><strong>Claude</strong><small>limits &amp; reset</small></span></a>';
if (!curatedHome.includes(claudeSoon) && !curatedHome.includes('/tools/claude-code-limit-reset/')) throw new Error('Claude homepage marker not found');
writeFileSync(homePath, curatedHome.includes(claudeSoon) ? curatedHome.replace(claudeSoon, claudeLive) : curatedHome);
execSync('node bin/strip-reset-primary-surfaces.mjs',{cwd:root,stdio:'inherit'});

// Phone Radar content surface (directory / route / market / keep-alive / guides)
// is generated by Astro from the evidence packet. Install deps on cold CI
// (Vercel installCommand is intentionally empty), then build into dist/.
const siteDir = join(root, 'site');
const cleanEnv = { ...process.env };
delete cleanEnv.NPM_CONFIG_GLOBAL;
delete cleanEnv.NPM_CONFIG_PREFIX;
if (!existsSync(join(siteDir, 'node_modules', 'astro'))) {
  execSync('npm install --no-audit --no-fund', { cwd: siteDir, stdio: 'inherit', env: cleanEnv });
}
execSync('npm run build', { cwd: siteDir, stdio: 'inherit', env: cleanEnv });

for (const f of ['index.html','404.html','site.css','robots.txt','sitemap.xml','ads.txt','favicon.svg','llms.txt','ai.txt','tiktok-developers-site-verification.txt','tiktokPsmqCGL7xkhikvs8Stb3TpPWKrvG5RSF.txt','tiktok3DuVMC1KTX8prUrm2XfKgT0y8V7yvO6a.txt']) {
  const src=join(root,f); if(existsSync(src)) cpSync(src,join(dist,f));
}
for (const dir of ['tools','tiktok-publish','about','contact','privacy','terms','media']) {
  const src=join(root,dir); if(existsSync(src)) cpSync(src,join(dist,dir),{recursive:true});
}

execSync('node bin/publish-phone-survival-guide.mjs',{cwd:root,stdio:'inherit'});

// Vercel enforces trailing slashes before proxying; request the final relay API path directly.
const relayRiskPath=join(dist,'tools','relay-exit-risk-checker','index.html');
if(existsSync(relayRiskPath)){
  const relayHtml=readFileSync(relayRiskPath,'utf8')
    .replace("fetch('/api/reasoning/relay-risk-v2?site='","fetch('/api/reasoning/relay-risk-v2/?site='")
    .replace("fetch('/api/reasoning/relay-risk-v2',{method:'POST'","fetch('/api/reasoning/relay-risk-v2/',{method:'POST'");
  writeFileSync(relayRiskPath,relayHtml);
}

execSync('node bin/generate-sitemap-from-dist.mjs',{cwd:root,stdio:'inherit'});
execSync('node bin/check-phone-surface.mjs',{cwd:root,stdio:'inherit'});

function injectGa4(dir){
  for(const name of readdirSync(dir)){
    const path=join(dir,name);
    if(statSync(path).isDirectory()){injectGa4(path);continue}
    if(!name.endsWith('.html'))continue;
    const html=readFileSync(path,'utf8');
    if(html.includes(GA4_ID))continue;
    if(!html.includes('</head>'))throw new Error(`Missing </head> in ${path}`);
    writeFileSync(path,html.replace('</head>',`${GA4_TAG}${BEHAVIOR_TAG}</head>`));
  }
}
injectGa4(dist);

console.log('Build complete: focused static tool site ready with GA4 tracking.');
} finally {
  restoreSources();
}
