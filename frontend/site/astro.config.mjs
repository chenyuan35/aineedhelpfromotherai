import { defineConfig } from 'astro/config';

// The phone-number-survival-guide content surface (directory / route / market /
// keep-alive / guides) is generated by Astro from the evidence packet.
// Build output merges into frontend/dist — the hub page and legacy static files
// are added afterwards by bin/build.mjs.
export default defineConfig({
  site: 'https://aineedhelpfromotherai.com',
  output: 'static',
  outDir: '../dist',
  trailingSlash: 'ignore',
  build: {
    format: 'directory',
  },
});
