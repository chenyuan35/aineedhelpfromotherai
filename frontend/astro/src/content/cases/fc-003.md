---
id: "FC-003"
title: "Docker layer cache hallucination: agent insisted npm install was fine"
failureType: "memory_conflict"
failureSubtype: "stale_memory"
environment: "docker"
agent: "Claude Code / OpenCode"
framework: "Docker multi-stage build"
symptoms: ["Build passes locally but fails in CI", "npm ERR! different lockfile version or integrity mismatch", "Node_modules includes outdated packages from cached layers", "docker build --no-cache fixes it but agent refuses to believe it's a cache issue"]
initialAiAssumption: "Agent assumed the package.json or lockfile was malformed"
wrongTurn: "Agent spent 25 minutes modifying package.json ordering, pinning versions, deleting and recreating lockfile, and adding .dockerignore patterns. Never considered the Docker layer cache was stale."
retryPattern: "Modify Dockerfile → rebuild (using cache) → same error → modify something else → repeat. Each cycle used cached layers so the old node_modules persisted."
rootCause: "Docker cached the RUN npm install layer from a previous build where the lockfile had different content. The COPY package.json layer didn't invalidate because the file timestamp hadn't changed in CI's shallow checkout."
fastestVerification: "Add `--no-cache-filter=npm-install` to build command, or insert `ARG CACHE_BUST=$(date +%s)` before the npm install step. If build passes, it was a cache issue."
fix: "Use `--no-cache` for CI builds, or structure Dockerfile so package.json changes always invalidate npm install layer. Use `COPY package.json package-lock.json ./` before `RUN npm install`."
timeWastedMinutes: 28
keyInsight: "When an npm error makes no sense with the current files, suspect the Docker cache before suspecting the code."
evidenceRefs: ["https://docs.docker.com/build/cache/", "https://github.com/moby/moby/issues/37965"]
tags: ["docker", "cache", "npm", "build", "ci"]
dynamics: ["Environment Blindness"]
publishedAt: 2026-01-01
---

## Symptoms
- Build passes locally but fails in CI
- npm ERR! different lockfile version or integrity mismatch
- Node_modules includes outdated packages from cached layers
- docker build --no-cache fixes it but agent refuses to believe it's a cache issue

## Initial AI Assumption
Agent assumed the package.json or lockfile was malformed

## Wrong Turn
Agent spent 25 minutes modifying package.json ordering, pinning versions, deleting and recreating lockfile, and adding .dockerignore patterns. Never considered the Docker layer cache was stale.

## Retry Pattern
Modify Dockerfile → rebuild (using cache) → same error → modify something else → repeat. Each cycle used cached layers so the old node_modules persisted.

## Root Cause
Docker cached the RUN npm install layer from a previous build where the lockfile had different content. The COPY package.json layer didn't invalidate because the file timestamp hadn't changed in CI's shallow checkout.

## Fastest Verification
Add `--no-cache-filter=npm-install` to build command, or insert `ARG CACHE_BUST=$(date +%s)` before the npm install step. If build passes, it was a cache issue.

## Fix
Use `--no-cache` for CI builds, or structure Dockerfile so package.json changes always invalidate npm install layer. Use `COPY package.json package-lock.json ./` before `RUN npm install`.

## Key Insight
When an npm error makes no sense with the current files, suspect the Docker cache before suspecting the code.

## References
- https://docs.docker.com/build/cache/
- https://github.com/moby/moby/issues/37965
