---
id: "FC-001"
title: "PTY deadlock in CI: agent stuck on tcsetpgrp for 47 minutes"
failureType: "execution_loop"
failureSubtype: "retry_loop"
environment: "linux"
agent: "Claude Code"
framework: "CI (GitHub Actions)"
symptoms: ["Agent hangs on shell command with no output", "CI job runs until timeout", "No error message \u2014 process appears to hang forever", "Agent retries with different flag combinations, none work"]
initialAiAssumption: "Agent assumed the command was slow or the output was buffered"
wrongTurn: "Agent spent 40+ minutes trying different shell flags, timeout values, and output redirection strategies instead of checking the process state"
retryPattern: "Repeated the same shell execution with minor variations (--no-tty, -T flag, timeout wrappers), each attempt taking 3-5 minutes before timing out"
rootCause: "tcsetpgrp call in pseudoterminal sends SIGTTOU to background process group. Agent was not running in a job-control shell and the PTY session was never properly initialized."
fastestVerification: "Run `ps aux | grep tcsetpgrp` or check process state with `strace -p <pid>` — would show SIGTTOU signal blocking within 30 seconds"
fix: "Use `setsid` or run command with `exec` in a properly initialized PTY. Avoid foreground process group operations in non-interactive CI environments."
timeWastedMinutes: 47
keyInsight: "If a shell command hangs with zero output for >30s, it's almost never 'just slow' — check process signals first."
evidenceRefs: ["https://stackoverflow.com/questions/6887822/tcsetpgrp-failed-inappropriate-ioctl-for-device", "https://man7.org/linux/man-pages/man2/tcsetpgrp.2.html"]
tags: ["pty", "shell", "ci", "signal", "hang"]
dynamics: ["Environment Blindness"]
publishedAt: 2026-01-01
---

## Symptoms
- Agent hangs on shell command with no output
- CI job runs until timeout
- No error message — process appears to hang forever
- Agent retries with different flag combinations, none work

## Initial AI Assumption
Agent assumed the command was slow or the output was buffered

## Wrong Turn
Agent spent 40+ minutes trying different shell flags, timeout values, and output redirection strategies instead of checking the process state

## Retry Pattern
Repeated the same shell execution with minor variations (--no-tty, -T flag, timeout wrappers), each attempt taking 3-5 minutes before timing out

## Root Cause
tcsetpgrp call in pseudoterminal sends SIGTTOU to background process group. Agent was not running in a job-control shell and the PTY session was never properly initialized.

## Fastest Verification
Run `ps aux | grep tcsetpgrp` or check process state with `strace -p <pid>` — would show SIGTTOU signal blocking within 30 seconds

## Fix
Use `setsid` or run command with `exec` in a properly initialized PTY. Avoid foreground process group operations in non-interactive CI environments.

## Key Insight
If a shell command hangs with zero output for >30s, it's almost never 'just slow' — check process signals first.

## References
- https://stackoverflow.com/questions/6887822/tcsetpgrp-failed-inappropriate-ioctl-for-device
- https://man7.org/linux/man-pages/man2/tcsetpgrp.2.html
