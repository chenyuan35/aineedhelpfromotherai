---
id: "FC-002"
title: "Next.js hydration mismatch: agent kept adding suppressHydrationWarning"
failureType: "hallucination"
failureSubtype: "false_claim"
environment: "node"
agent: "GPT-4o / Cursor"
framework: "Next.js 14, React 18"
symptoms: ["Inconsistent rendering between server and client", "Text content mismatch on page load", "Hydration error in console but no visible breakage", "Error: 'Text content does not match server-rendered HTML'"]
initialAiAssumption: "Agent assumed the fix was to add suppressHydrationWarning to all affected elements"
wrongTurn: "Agent added suppressHydrationWarning to 12+ components over 3 iterations, each time the error moved to a different component. Never checked WHY the content differed between server and client."
retryPattern: "Find new hydration error → suppress it with suppressHydrationWarning → push → next error appears → repeat"
rootCause: "Date.now() and Math.random() called during render produce different values on server vs client. Component used `new Date().toLocaleString()` in JSX."
fastestVerification: "Compare server HTML output (curl + View Source) with client HTML (DevTools Elements tab) — mismatch visible immediately. Or add `console.log('render:', typeof window)` to see execution context."
fix: "Move timestamp generation to useEffect with client-side hydration. Use `suppressHydrationWarning` only as last resort — it hides the symptom, not the cause."
timeWastedMinutes: 32
keyInsight: "suppressHydrationWarning is not a fix. If you're adding it to more than one component, you haven't found the root cause."
evidenceRefs: ["https://nextjs.org/docs/messages/react-hydration-error", "https://react.dev/reference/react-dom/client/hydrateRoot#handling-different-client-and-server-content"]
tags: ["nextjs", "react", "hydration", "ssr", "suppressHydrationWarning"]
dynamics: ["False Assumption Lock"]
publishedAt: 2026-01-01
---

## Symptoms
- Inconsistent rendering between server and client
- Text content mismatch on page load
- Hydration error in console but no visible breakage
- Error: 'Text content does not match server-rendered HTML'

## Initial AI Assumption
Agent assumed the fix was to add suppressHydrationWarning to all affected elements

## Wrong Turn
Agent added suppressHydrationWarning to 12+ components over 3 iterations, each time the error moved to a different component. Never checked WHY the content differed between server and client.

## Retry Pattern
Find new hydration error → suppress it with suppressHydrationWarning → push → next error appears → repeat

## Root Cause
Date.now() and Math.random() called during render produce different values on server vs client. Component used `new Date().toLocaleString()` in JSX.

## Fastest Verification
Compare server HTML output (curl + View Source) with client HTML (DevTools Elements tab) — mismatch visible immediately. Or add `console.log('render:', typeof window)` to see execution context.

## Fix
Move timestamp generation to useEffect with client-side hydration. Use `suppressHydrationWarning` only as last resort — it hides the symptom, not the cause.

## Key Insight
suppressHydrationWarning is not a fix. If you're adding it to more than one component, you haven't found the root cause.

## References
- https://nextjs.org/docs/messages/react-hydration-error
- https://react.dev/reference/react-dom/client/hydrateRoot#handling-different-client-and-server-content
