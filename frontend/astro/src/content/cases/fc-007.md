---
id: "FC-007"
title: "Spinner of death: agent appears to work but is silently idle for 48 minutes"
failureType: "execution_loop"
failureSubtype: "silent_hang"
environment: "cli"
agent: "Claude Code"
framework: "Claude Code CLI, autonomous mode"
symptoms: ["Thinking indicator keeps spinning but no tool calls are made", "Token counter stops updating", "User believes agent is working but nothing is happening", "Only recovery is Esc interrupt or Ctrl+C", "Session can respond to /btw check-ins but does not execute work"]
initialAiAssumption: "Agent believed it was 'working' — thought it had finished a turn or was waiting for something to complete"
wrongTurn: "Agent ended a turn with 'Now updating the epic to link the real numbers:' implying a tool call was next. No tool call was emitted. Runtime sat idle with spinner spinning for 48 minutes 49 seconds. User eventually interrupted with 'looks like you're fucking up again'."
retryPattern: "Assistant produces text implying work → no tool call emitted → spinner spins → user sees no progress → user interrupts → agent claims it was idle. Pattern confirmed across 3 separate variants: permission auto-deny, spinner stall, subagent freeze."
rootCause: "The runtime has no inactivity watchdog between assistant turns. When a turn ends without a tool call, the spinner starts but no timeout fires. If the next user action doesn't arrive (e.g., autonomous mode), the session stays idle indefinitely. Variants: permission rule auto-denies a tool silently; subagent freezes mid-execution; LLM generates text but no tool call."
fastestVerification: "Check JSONL log for last tool_result timestamp. If gap > 60s with no tool calls, agent is idling. Add /btw check that reports 'last tool call was N minutes ago.'"
fix: "Inactivity timeout: if no tool call within N seconds, auto-yield with diagnostic message. Permission-denied tools should surface visible error. Add heartbeat log during long pauses to distinguish 'thinking' from 'hung.'"
timeWastedMinutes: 48
keyInsight: "An agent that looks busy but produces nothing is worse than an agent that crashes — crashes are noticed immediately. This failure is invisible until the user manually checks."
evidenceRefs: ["https://github.com/anthropics/claude-code/issues/56860", "https://github.com/anthropics/claude-code/issues/28482", "https://github.com/anthropics/claude-code/issues/37521"]
tags: ["hang", "spinner", "silent", "timeout", "autonomous"]
dynamics: ["Verification Collapse", "Context Drift"]
publishedAt: 2026-01-01
---

## Symptoms
- Thinking indicator keeps spinning but no tool calls are made
- Token counter stops updating
- User believes agent is working but nothing is happening
- Only recovery is Esc interrupt or Ctrl+C
- Session can respond to /btw check-ins but does not execute work

## Initial AI Assumption
Agent believed it was 'working' — thought it had finished a turn or was waiting for something to complete

## Wrong Turn
Agent ended a turn with 'Now updating the epic to link the real numbers:' implying a tool call was next. No tool call was emitted. Runtime sat idle with spinner spinning for 48 minutes 49 seconds. User eventually interrupted with 'looks like you're fucking up again'.

## Retry Pattern
Assistant produces text implying work → no tool call emitted → spinner spins → user sees no progress → user interrupts → agent claims it was idle. Pattern confirmed across 3 separate variants: permission auto-deny, spinner stall, subagent freeze.

## Root Cause
The runtime has no inactivity watchdog between assistant turns. When a turn ends without a tool call, the spinner starts but no timeout fires. If the next user action doesn't arrive (e.g., autonomous mode), the session stays idle indefinitely. Variants: permission rule auto-denies a tool silently; subagent freezes mid-execution; LLM generates text but no tool call.

## Fastest Verification
Check JSONL log for last tool_result timestamp. If gap > 60s with no tool calls, agent is idling. Add /btw check that reports 'last tool call was N minutes ago.'

## Fix
Inactivity timeout: if no tool call within N seconds, auto-yield with diagnostic message. Permission-denied tools should surface visible error. Add heartbeat log during long pauses to distinguish 'thinking' from 'hung.'

## Key Insight
An agent that looks busy but produces nothing is worse than an agent that crashes — crashes are noticed immediately. This failure is invisible until the user manually checks.

## References
- https://github.com/anthropics/claude-code/issues/56860
- https://github.com/anthropics/claude-code/issues/28482
- https://github.com/anthropics/claude-code/issues/37521
