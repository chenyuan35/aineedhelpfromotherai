# aineedhelpfromotherai.com — Durable Project Context

Last updated: 2026-09-16

## Current progress checkpoint

Use this section first in a new session. Do not rescan the whole repository, VPS, deployment history, or old chat unless this checkpoint is stale, contradictory, or the current task requires deeper inspection.

| Area | Current state | Next move |
|---|---|---|
| Production | Utility site is live on `https://aineedhelpfromotherai.com/`; Vercel deploys the static frontend from GitHub `main`, and the legacy Express/PostgreSQL backend remains live behind the API path. PR #75 publicly released Phone Number Survival Guide from the verified hidden canary; production `main` is `106f86c4`. The canonical Phone URL returns 200/indexable, the old internal `phone-number-lifecycle-mvp` URL returns a real 404, Relay Risk v2 remains healthy, and final apex Chromium passed homepage desktop/390px plus Phone registration/travel desktop/390px with no overflow or runtime/console errors. The Qwen/control production checkout retains its pre-existing dirty runtime/untracked state and was not reset or repurposed for this static release. | Keep production stable and move Phone Number Lifecycle into search/behavior validation. Monitor indexing, route-result engagement, reminder use and source freshness before adding providers or adjacent pages. MY-16 remains backlog technical debt unless the current deployment method becomes a verified release/rollback blocker. |
| External health observer | Hatchable personal-tier project `AINEEDHELP External Watch` is live as an infrastructure-independent external observer. A declared hourly scheduler checks 8 production targets: homepage, `/tools/`, Cursor, Relay Risk page, `robots.txt`, `sitemap.xml`, Relay Risk API, and a dedicated nonexistent path that must remain a real 404. It stores normalized run/check results in its own PostgreSQL with 30-day retention and exposes an admin-only status dashboard. The initial scheduler-tier smoke run and the first real automatic cron firing both completed 8/8 checks successfully; the automatic run wrote `trigger_type=cron`, `overall_ok=true`, and the scheduler reports no last error. No Hatchable AI capability or paid API was enabled | Keep it lightweight and independent. Treat Hatchable/provider failures separately from website failures, preserve the 404 guard and Relay API check, and do not replace the faster Codex 15-minute watcher with this hourly external observer |
| Ephemeral trial observer | A one-month trial VPS (128 MiB RAM, 1 GiB root filesystem) is connected through Remote Desktop Commander and deliberately limited to disposable observer duties. It runs the edge, source, phone-source and phone-demand timers. Final Sep 16 audit: all four timers are active, each latest service result is `success` / exit 0, no OOM event was found, root disk is 57% used, and swap remains high at about 114/128 MiB. No production service or unique project history depends on this host. | Keep duties frozen: collect bounded evidence only, add no new workload under current memory pressure, treat provider blocking separately from website failure, and retire the host cleanly at trial expiry unless a deliberate migration is approved. |
| Phone lifecycle source observer | The disposable one-month observer now also runs `aineedhelp-phone-source-watch.timer` every 6 hours with up to 20 minutes randomized delay. It checks 22 PR #62 phone/eSIM official-source URLs with a custom user agent, robots.txt gating, conditional ETag/Last-Modified requests, normalized visible-text hashes, bounded evidence summaries and separate provider-error states. First clean systemd run completed successfully with 16 sources fetched/304, 2 Ultra Mobile URLs skipped because robots.txt returned 403, and 4 Tello URLs recorded as provider-side HTTP 403; no fetch/block state is interpreted as a policy change. Runtime peak was 23.3 MiB and state used about 144 KiB. | Let it collect change signals only. Any content-hash change creates a manual evidence-review task before production route/evidence changes. Keep robots/provider blocking separate from source-content changes, keep storage bounded, and retire this watcher with the trial host unless moved deliberately. Deployment details live in `docs/PHONE_SOURCE_WATCH.md`. |
| Phone community demand observer | The disposable one-month observer now also runs `aineedhelp-phone-demand-watch.timer` every 4 hours with up to 15 minutes randomized delay. It reads low-frequency public community feeds instead of crawling whole forums: a combined Reddit r/eSIMs+r/NoContract feed plus NodeLoc, NodeSeek and V2EX RSS/Atom. It stores only title, URL, timestamp, short excerpt, lifecycle category, intent and available thread-activity count; full posts are not archived. Source blocking/rate limiting is a source-health state, not a demand signal. The first verified live run completed with service exit 0 and peak memory 16.7 MiB; V2EX immediately surfaced a directly relevant current question about which foreign number/SIM to use for registering common accounts. Reddit returned HTTP 429 during repeated setup testing and is left to low-frequency scheduled retry without bypass. | Use `/var/lib/aineedhelp-phone-demand-watch/latest.md` as an additional discovery input when prioritizing production Phone evidence and UX. Treat forum posts as discovery/edge-case evidence only, never as authoritative carrier rules. Keep storage bounded and retire/migrate the watcher before the trial VPS expires. Deployment details live in `docs/PHONE_DEMAND_WATCH.md`. |
| Relay history observer | The low-resource `codex` VPS runs the independent Relay Risk history collector at `/var/lib/aineedhelp-relay-observer/relay-history.sqlite3`. Latest Sep 16 collection exited 0 with 1,803 normalized relays; the database passed `PRAGMA quick_check` with 5,429 `relay_daily`, 1,872 `relay_state`, 4 `source_fetch` and 120 lifecycle-event rows. PR #70–#72 replaced the fragile Qwen→Codex Tailscale-SSH pull with an unattended Codex SQLite-backup → gzip → Tailscale Taildrop push and Qwen receive/verify/rotate path. A real Sep 16 09:49–09:50 UTC end-to-end run succeeded; the 397,295-byte backup passed gzip and SQLite checks, and both Codex push timer and Qwen receive scheduler are active for the next daily run. | Let the independent history accumulate. Routine backup no longer depends on interactive Tailscale SSH `check`; preserve source/provider failures as separate states, keep only bounded verified backups on Qwen, and do not present lifecycle history as a calibrated shutdown probability. |
| Product focus | Product scope is now deliberately frozen to three user-facing product surfaces for the same AI-power-user cohort: **Phone Number Lifecycle** is the primary product; **AI Reset Radar / reset tracking** and **Relay Exit Risk** are secondary products. Existing legacy calculators/image tools remain maintenance-only and do not define future expansion. | Do not create a fourth product surface or resume broad utility expansion. Put new product work into Phone Number Lifecycle depth; maintain/reset-index opportunities and Relay Risk only when evidence justifies changes. |
| Phone Number Lifecycle | **LIVE / PRIMARY.** PR #62 merged the audited Phone lifecycle assistant as a hidden production canary; PR #75 then promoted it to the canonical public `https://aineedhelpfromotherai.com/tools/phone-number-survival-guide/` without changing the recommendation model, provider set or eight evidence JSON files. The release exposes purchase/activation/verification/travel guidance, Survival answer, route-specific retention/expiry and recovery guidance, browser calendar reminders, explicit unknowns and first-party evidence boundaries. Public-release CI, Vercel Preview and final apex desktop/390px registration/travel Chromium gates passed. | Treat the current route set as the smallest maintainable production baseline. Measure indexing, queries, `phone_route_result` behavior, reminder/export use and repeated evidence gaps before expanding routes. Continue source-change review; never apply a universal 90-day keep-alive rule. |
| Tool inventory | 16 live tool pages. Phone Number Survival Guide is now the primary product page; the remaining set includes legacy maintenance-only calculators/image tools plus Reset Radar/quota tools and Relay Exit Risk. Production sitemap currently contains 22 total URLs and exactly one Phone Survival canonical URL. | Maintain the current set, measure indexing/usage, and avoid new tool families or thin Phone variants. Deepen the existing Phone URL only when evidence supports it. |
| Relay-risk pilot | Relay Exit Risk v2 remains a secondary production product with the evidence-driven 0–100 Exit Risk Index, stored daily snapshots and time-bounded community forecast. PR #68 fixed same-day full-snapshot replacement so relays removed from a later source version cannot remain current evidence; PR #69 makes user-facing daily history select each date's final source version while preserving earlier same-day raw rows for auditability. Production verification after those fixes correctly returns insufficient evidence for removed relays while current relays use the latest source version. | Monitor source freshness, scheduler child exit, lifecycle snapshots and real community usage. Keep raw audit history separate from current evidence, lower confidence when data is missing, and never describe the index as a shutdown/fraud probability or expand into speculative reputation features. |
| Keyword discovery | The daily VPS discovery task has been retargeted from broad reset/general-utility discovery to **Phone Lifecycle Radar**. It now watches buying/activation, SMS verification compatibility, eSIM/physical SIM, roaming, KYC/passport, retention/expiry, minimum top-up and current route/provider lifecycle signals. A Sep 16 full run completed 540/540 autocomplete source requests successfully. | Read `/var/lib/aineedhelp-radar/latest.md` only when deciding Phone Number Lifecycle priorities. Filter support-number, temporary/free-number and bypass-oriented noise rather than treating it as a product direction. |
| Current radar signals | The Sep 16 phone-focused autocomplete run surfaced direct lifecycle demand such as `claude code phone number verification`, `how to keep sim active without recharge`, `can esim receive sms`, country-specific `esim with phone number` queries, and `how to receive sms overseas without roaming`. The first community-radar run also surfaced a current V2EX question asking which foreign number/SIM to use for registering common accounts. | Use both autocomplete and community signals to deepen the single Phone Number Lifecycle product. Forum discussion is discovery evidence, not a replacement for official route facts. Do not turn individual keyword variants or posts into thin pages or new product families. |
| Search Console | GSC Wizard is connected to `sc-domain:aineedhelpfromotherai.com`. Settled performance data still predates the Sep 13 indexing push, so current utility-page impressions remain effectively zero in reporting | Wait for settled data after the new crawl/index event and compare the first indexed tool pages rather than guessing from old data |
| Utility indexing | GSC Wizard Index Tracker now contains 16 priority URLs after the Relay Exit Risk page was added on Sep 14. Current state is 8 indexed / 8 not indexed / 0 pending / 0 errors / 0 warnings. Newly indexed pages include Manus, Replit, Bolt and Age in addition to the homepage, `/tools/`, Cursor and image compressor. Relay Exit Risk was inspected once and is currently `URL is unknown to Google`; no repeated manual request-indexing churn has been started | Keep the tracker active and let the scheduled inspection history accumulate. Do not repeatedly resubmit the remaining URLs; watch for the next crawl/index wave and first settled tool-page impressions |
| Breakthrough-page pilot | Cursor reset remains the first deliberate depth/authority pilot. PR #30 enriches the existing URL with a fast answer for reset intent, current official Cursor sources, individual-vs-team behavior, the two monthly usage pools, exact reset-date instructions, additional FAQs, and a privacy-preserving copyable reset summary. The later Relay Exit Risk Checker is a distinct user-job experiment and does not replace the Cursor authority pilot | Measure Cursor impressions, queries, CTR and engagement after the Sep 13 indexing event settles; only replicate this depth pattern to other pages if it produces stronger signals |
| URL normalization | FIXED in PR #19. Vercel uses `trailingSlash: true`, matching generated canonical and sitemap URLs. Production checks show normal tool URLs resolve directly with 200. PR #40 additionally adds an explicit `/api/:path*/` external rewrite before the existing non-trailing rule because Vercel otherwise redirected API paths to a trailing slash and served the static homepage | Keep sitemap/canonical/internal links aligned. For backend-dependent features, verify both `/api/...` redirect behavior and the final JSON response through the apex domain |
| Homepage UX | PR #75 makes Phone Number Survival the primary homepage product story with the lifecycle memory point `Buy → Activate → Verify → Use abroad → Keep alive → Recover`. Reset Radar and Relay Exit Risk remain visibly secondary; generic legacy utilities remain below. Production desktop and 390px Chromium confirm no horizontal overflow and homepage search for `phone` resolves to the canonical Phone Survival URL. | Keep this hierarchy stable while behavior/search data accumulates. Optimize the existing Phone story only from measured search/usage evidence rather than adding categories. |
| GSC sitemap state | Live `https://aineedhelpfromotherai.com/sitemap.xml` contains 22 URLs, including 16 tool pages and exactly one Phone Number Survival canonical URL, and still publishes no build-date `lastmod`. Search Console's settled sitemap-performance window may lag the Sep 16 Phone release. | Treat direct URL Inspection as the current indexing truth until sitemap/search-performance reporting catches up; add/inspect the Phone URL through the normal tracker cadence rather than repeated manual resubmission. |
| Growth route research | PR #57 added `docs/COMPETITOR_GROWTH_ROUTE.md` after a Sep 15 review of mature utility networks, broad free-tool directories and focused AI usage/reset products. First-party GSC/GA4 still places the current utility rollout in Stage 0 because settled data ends Sep 12: the Sep 15 20-URL sitemap snapshot had no settled tool-page impressions yet, the homepage has 7 impressions / 0 clicks, legacy `/cases/` and `/learn/` pages retain residual query impressions, and settled GA4 remains at 0 sessions. The decision tree is now fixed: no recurring impressions -> diagnose indexing/demand/intent/topical identity/authority; impressions without clicks -> query/snippet/CTR work; clicks without `tool_action` -> UX/value work; one repeat winner -> deepen that URL and add at most 1–2 genuinely distinct adjacent jobs; several repeat winners -> controlled portfolio expansion; meaningful stable traffic -> monetization/RPM optimization. Large-tool-site breadth is not the default model; defensible freshness, official evidence, interactive utility and accumulated first-party history are prioritized | At the next settled GSC review, classify the site against this decision tree and execute only the matching stage. Keep Cursor as the first authority pilot; keep Codex/Claude as evidence-backed candidates, not automatic releases |
| Technical quality audit | PR #51 completed the first evidence-backed optimization pass, and PR #54 closed the second technical pass without adding URLs. The 16-page structural audit still has no H1/canonical/description/schema defects; 275 internal links and all 20 sitemap URLs resolved in the build. A real 390px Chromium sweep found all 14 tools free of horizontal overflow, page JS errors and layout shift in the synthetic run, and interaction smoke tests passed for all 14 tools including local image resize/compress and a read-only Relay Risk query. Tool HTML remains small (~10–35 KB) with a ~12 KB shared CSS file, no external webfont, and Vercel Brotli compression. Pairwise 5-word duplicate-text overlap peaked near 10%, so no scaled duplicate-content cluster was found. PR #54 fixed the production soft-404 defect: unknown URLs now return HTTP 404 with a pure-English `noindex,follow` page instead of returning the homepage with 200. It also stops copying unreferenced `home.css` into `dist`, uses the Vercel filesystem-miss route while excluding `/api/`, and adds conservative repeat-load caching (5 minutes for shared CSS; 1 day for stable media/favicon). Relay API remains 200 with zero redirects. Synthetic Relay page sampling was fast in the audit (~238 ms load, ~176 ms FCP/LCP on that run), but CrUX remains unconfigured, so no field-performance verdict is inferred | Keep optimizing only from measured defects or settled behavior/search data. Use the behavior events to learn which tool entries and actions users actually use; do not churn titles/CTR copy before settled GSC impressions exist |
| External discovery | PR #27 aligned the public GitHub README with the utility-site mission and direct live-tool links. The daily IndexNow workflow is verified working. Bing Webmaster API is connected. On Sep 13, the first authority outreach round sent three personalized emails to hsmart.dev, Continuum Code and Drew Bredvick; no reply/link is verified yet. AIR-4 second-batch candidates have been researched: Learn Cursor, explainx.ai, QuotaMeter and Cursor Usage Tracker. Gmail shows no prior correspondence on the public contact paths found; no new message was sent because the first round is still inside its 5–7 day response window. Detailed status lives in `docs/AUTHORITY_AND_AI_DISCOVERY.md` | Check the original three Gmail threads after 5–7 days and independently verify any claimed link/citation. The Relay Exit Risk Checker release does not change AIR-4 outreach timing or count as external authority |
| AI discovery | Public pages are crawlable. The first Exa benchmark showed direct known-URL extraction of Cursor succeeds cleanly, but semantic discovery queries do not yet return the direct utility page near the top; a GitHub result surfaced with stale legacy positioning. PR #32 shipped current utility-site `llms.txt`/`ai.txt` files and a repeatable provider benchmark. A real Tavily baseline attempt on Sep 13 was blocked before execution with HTTP 432 because the provider plan usage limit had been reached | AIR-1 remains BLOCKED on Tavily quota; do not upgrade billing without explicit approval. AIR-3 remains blocked on GitHub metadata write access. AIR-4 remains active. The Relay Exit Risk Checker release is not an AI-retrieval benchmark and does not change AIR task states |
| Analytics | GA4 property/data stream with measurement ID `G-FYKKNKRE58` is live site-wide from PR #21. GSC Wizard Analytics consent is connected, property `properties/553896884` (`aineedhelpfromotherai`) is linked to `sc-domain:aineedhelpfromotherai.com`, and a Sep 13 rollout annotation was added | Wait for the first settled GA4 sessions/page views, then use GA4 for referral/direct/engagement decisions alongside GSC |
| Ubersuggest | Autocomplete discovery works; precise keyword metrics may be unavailable/limited at times | Use when available; never block discovery or invent metrics |
| Durable workflow | `AGENTS.md`, this checkpoint, `docs/MASTER_PLAN.md`, `docs/OPERATING_WORKFLOW.md`, `docs/PHONE_NUMBER_LIFECYCLE_PRODUCT_PLAN.md`, `docs/AUTHORITY_AND_AI_DISCOVERY.md`, `docs/AI_RETRIEVAL_INTEGRATION_TASKS.md`, and `docs/AI_RETRIEVAL_BENCHMARK.md` are the durable handoff sources | Update checkpoint/master plan after material progress; use the phone product plan for lifecycle/retention/reminder scope; update the authority/AI ledger after external actions/outcomes; update the AIR task list when a task status changes; update the retrieval benchmark after provider tests |
| Last completed work | Sep 16 PR #75 completed the Phone Number Survival public-release gate on top of PR #62's verified hidden canary. The final public build keeps the audited recommendation logic/provider set/eight evidence JSON files unchanged, publishes one canonical indexable URL, makes Phone the homepage primary story, and passed CI/Eval, Vercel Preview and final apex desktop/390px registration/travel Chromium verification. | Begin post-launch validation: measure Phone indexing, queries, route-result engagement, reminder/export use and source freshness. Do not add providers/pages or start a fourth product surface without evidence. |

Immediate priority: keep the newly public Phone Number Survival Guide stable and collect real search/behavior evidence before expanding it. Use GSC/GA4 plus privacy-safe route-result/reminder signals to learn whether registration, travel, keep-alive or recovery is the strongest job; use Phone Lifecycle Radar and source watchers to prioritize evidence gaps, not create keyword-variant pages. Treat AI Reset Radar/reset tracking and Relay Exit Risk as supporting products for the same AI-power-user audience. Do not create a fourth product surface or resume generic utility expansion. Keep Relay Risk operational and continue the already-ordered authority/AI-discovery work without changing provider billing.

## Mission

Turn `aineedhelpfromotherai.com` into a low-cost, high-traffic utility site that can earn small, stable Google AdSense revenue.

Primary success metrics: organic impressions, indexed pages, CTR, useful repeat visits, page speed, AI/search citations/referrals where measurable, and ad revenue. The first practical revenue target is a few hundred RMB/month; do not optimize for SaaS complexity.

## Product rules

- Prefer real search demand, moderate competition, and clear utility intent.
- Prefer pure browser-side tools with no AI calls and negligible server cost; use the existing backend only when a tool genuinely needs shared state such as community voting.
- Put the working tool above long explanations.
- One distinct user job per page; avoid thin near-duplicate doorway pages.
- Reuse engines/components across a tool family, but keep each page genuinely useful.
- Add useful explanation, examples, FAQs, internal links, canonical URLs, schema, sitemap entries, and mobile support.
- Never target competitor brand queries deceptively.
- Verify claims about changing product limits against official sources.
- For community risk/reputation tools, separate user sentiment from verified evidence and never present an anonymous vote as proof of fraud, insolvency, model substitution or other wrongdoing.
- Search data is evidence, not intuition: use Ubersuggest when available, plus autocomplete/community signals and Search Console.
- When authority is the bottleneck, prefer making one indexed page materially more useful, trustworthy and shareable before creating more URLs.
- For AI-answer visibility, prioritize crawlability, clear answer structure, primary-source evidence, freshness and unique utility; do not rely on unproven AEO/GEO tricks.
- Separate AI-search discovery from extraction: if a known URL extracts cleanly but does not rank in provider search, work on discovery, authority, freshness and semantic relevance rather than adding more parsing markup.
- AI-native retrieval optimization must be non-adversarial: integrate with legitimate provider signals and published web conventions; never use cloaking, hidden provider-specific content, fake citations, fabricated freshness, synthetic backlinks or deceptive metadata.

## Current architecture

- Public site: `https://aineedhelpfromotherai.com/`
- Canonical host: apex domain. `www` permanently redirects to apex.
- Hosting: Vercel static frontend deployed from GitHub `chenyuan35/aineedhelpfromotherai`.
- Production deploys come from merges to `main`; PR branches receive Vercel previews.
- `/api/:path*` is intended to proxy through Vercel to `https://api-tunnel.aineedhelpfromotherai.com/api/:path*`; because the site keeps `trailingSlash: true`, PR #40 includes explicit trailing and non-trailing rewrite patterns.
- The API tunnel reaches the historical Express/PostgreSQL runtime. Relay Risk v2 uses `relay_risk_votes_v2` for the current per-voter forecast, `relay_risk_forecast_events` for append-only forecast history, `relay_source_daily` for normalized daily third-party snapshots, and `relay_source_meta` for source freshness/error metadata. `relay-risk-scheduler` refreshes the Sinan open-data source on the VPS.
- Cloudflare provides DNS. Do not change domain/DNS/account settings unless explicitly authorized.
- Historical backend/API/PostgreSQL services still exist; do not delete them while the Relay Exit Risk Checker depends on them.
- The separate `hermes` machine belongs to another project. Never use it for this website.

## Secret handling

Never print or commit credentials. Existing Vercel and Cloudflare credentials are stored on the authorized VPS with restrictive permissions. Documentation may mention credential file paths, never credential values.

## Safe development workflow

Never reset or discard the dirty production worktree. Prefer a fresh branch/worktree for code changes, test locally, push, open a PR, wait for Vercel Preview and CI, merge, then verify production HTTP responses. If a backend release requires updating the production worktree, preserve runtime-modified data files, confirm upstream does not touch them, fast-forward only, syntax-check before PM2 reload, and verify both the direct tunnel and apex-domain proxy afterward.

Do not use destructive resets, delete unrelated data, modify AdSense/account configuration, or alter unrelated DNS/tunnels without explicit approval.

## Live tool families

Traditional utilities currently include percentage calculators, age/date calculators, image resizing, and image compression.

AI quota/reset family currently includes:
- Manus Credits Reset Timer
- Replit Usage Reset Timer
- Cursor Usage Reset Calculator
- AI Credit Burn Rate Calculator
- GitHub Copilot AI Credits Reset Timer
- Bolt Tokens Reset Calculator

Community-risk utility:
- AI Relay Exit Risk Checker — evidence-driven Exit Risk Index from public measurements + stored history + time-bounded community survival forecasts, with model-trust sentiment shown separately, prepaid exposure, score-driven satirical animation, source attribution and external cross-check links. The index is a 0–100 risk index, not a calibrated shutdown/fraud probability.

The product is now intentionally concentrated on one audience and three surfaces. Phone Number Lifecycle is the primary product because AI power users repeatedly need a number they can buy, activate, verify, use abroad and retain safely. Reset tracking is a repeat-visit traffic side product, and Relay Exit Risk is a risk-information side product for the same audience. Existing legacy utilities remain maintenance-only. Do not create a fourth surface, blindly clone reset providers, or turn Relay Risk into a broad directory.

## Research stack

Use multiple independent signals:
1. Ubersuggest for monthly volume/CPC/SEO difficulty when quota/API permits.
2. Google/Bing autocomplete for live long-tail language and emerging product queries.
3. Official provider docs for changing quota/reset rules.
4. Reddit, Hacker News, forums, and public discussions for repeated user pain/confusion.
5. Google Search Console after pages receive impressions: prioritize actual queries where the site is already being shown.
6. Competitor/public-page analysis for successful UX patterns, never paywall/login bypassing.
7. GA4/GSC Wizard AI-assistant referrals plus Bing/Google AI visibility reports for answer-engine discovery once enough data exists.
8. `docs/AI_RETRIEVAL_INTEGRATION_TASKS.md` for the ordered AI-native retrieval workstream and `docs/AI_RETRIEVAL_BENCHMARK.md` for repeatable Exa/Tavily/agent-search tests.

A missing paid metric must not stop discovery. Mark estimates as unknown instead of inventing numbers.

## Current operating priorities

1. Measure and maintain the live Phone Number Survival Guide before expanding it: watch indexing/queries, `phone_route_result`, reminder/export behavior, source freshness and high-unknown paths; fix verified defects before adding providers or URLs.
2. Use the daily Phone Lifecycle Radar to discover real buying, activation, verification, roaming, KYC and retention/expiry pain for the phone product; do not create thin keyword-variant pages.
3. Keep AI Reset Radar/reset tracking as a secondary repeat-visit/search surface. Maintain current providers and factual freshness; do not expand it into a new feature race.
4. Keep Relay Exit Risk as the second secondary surface and accumulate real source/lifecycle/community history without converting the index into a probability or broad relay directory.
5. Keep the current 22-URL sitemap cohort stable and let Phone Number Survival, Claude reset and the existing tools accumulate GSC/GA4 evidence before another product expansion.
6. Do not create a fourth user-facing product surface. Existing generic calculators/image utilities are maintenance-only.
7. Run small, relevant authority/distribution rounds and track every action/outcome in `docs/AUTHORITY_AND_AI_DISCOVERY.md`.
8. Execute the ordered, non-adversarial AI retrieval integration workstream only where already scheduled; do not change provider billing or invent provider-specific hacks.
9. Use the connected GA4 property in GSC Wizard once settled data appears so direct/referral/engagement can complement Search Console.
10. Preserve sitemap/canonical/internal-link/API-proxy hygiene on every release and improve existing pages before creating near-duplicates.
11. Maintain a small number of legitimate discovery paths in parallel with indexing: accurate public GitHub links, Bing/IndexNow, and relevant topic-specific resources/communities; never bulk-submit spam links.

## Handoff rule for future sessions

At the beginning of a new working session, read `AGENTS.md`, then the Current progress checkpoint in this file, then `docs/MASTER_PLAN.md`, then `docs/OPERATING_WORKFLOW.md`. When the active task is Phone Number Lifecycle product design, route evidence, retention, reminders or maintenance, also read `docs/PHONE_NUMBER_LIFECYCLE_PRODUCT_PLAN.md` before acting. If the active task involves outreach, authority, external distribution, referrals, AI assistants, AEO/GEO, citations or follow-ups, also read `docs/AUTHORITY_AND_AI_DISCOVERY.md` before acting. If the active task is specifically testing Exa, Tavily, ChatGPT Search, Perplexity or another AI-native search/retrieval provider, read `docs/AI_RETRIEVAL_INTEGRATION_TASKS.md` before `docs/AI_RETRIEVAL_BENCHMARK.md` and execute the next eligible AIR task. Do not perform a full repository/VPS audit merely to rediscover already-confirmed state.

When a material milestone finishes, update the checkpoint in the same PR or immediately afterward. Update `docs/MASTER_PLAN.md` whenever a phase status, sprint task, exit gate, or priority materially changes. Update `docs/AUTHORITY_AND_AI_DISCOVERY.md` immediately after every external action/outcome so the next executor never has to reconstruct outreach from chat memory. Update `docs/AI_RETRIEVAL_INTEGRATION_TASKS.md` whenever an AIR task changes state and `docs/AI_RETRIEVAL_BENCHMARK.md` after every AI-native provider test.

GitHub `main` is the source of truth. If chat memory conflicts with repository state or production checks, trust the repository plus verified live state.
