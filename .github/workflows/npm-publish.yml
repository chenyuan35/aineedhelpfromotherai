name: npm Publish
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: Optional version bump (patch/minor/major). Leave empty to publish package.json versions.
        required: false
        default: ""

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: ${{ secrets.NPM_TOKEN && 'https://registry.npmjs.org' || 'https://npm.pkg.github.com' }}
      - run: npm ci || npm install --omit=dev
      - name: Skip root app package
        run: |
          echo "The repository root is the deployed website/backend app, not an npm integration package."
          echo "Publishing is limited to packages/mcp-bridge, packages/n8n-node, and packages/langchain-tool."
      - name: Publish MCP bridge package
        working-directory: packages/mcp-bridge
        run: |
          REQUESTED_BUMP="${{ github.event.inputs.version || '' }}"
          if [ -n "$REQUESTED_BUMP" ]; then
            npm version "$REQUESTED_BUMP" --no-git-tag-version
          fi
          VER=$(node -p "require('./package.json').version")
          NAME=$(node -p "require('./package.json').name")
          if [ -n "${{ secrets.NPM_TOKEN }}" ]; then
            PUBLISH_NAME="$NAME"
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on npm, skipping"
              exit 0
            fi
            echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
            npm publish --access public
          else
            PUBLISH_NAME="@chenyuan35/aineedhelpfromotherai-mcp"
            echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" > ~/.npmrc
            npm config set registry https://npm.pkg.github.com
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on GitHub Packages, skipping"
              exit 0
            fi
            jq --arg name "$PUBLISH_NAME" '.name = $name | .publishConfig.registry = "https://npm.pkg.github.com"' package.json > package.json.tmp
            mv package.json.tmp package.json
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}
      - name: Publish n8n community node
        working-directory: packages/n8n-node
        run: |
          REQUESTED_BUMP="${{ github.event.inputs.version || '' }}"
          if [ -n "$REQUESTED_BUMP" ]; then
            npm version "$REQUESTED_BUMP" --no-git-tag-version
          fi
          VER=$(node -p "require('./package.json').version")
          NAME=$(node -p "require('./package.json').name")
          if [ -n "${{ secrets.NPM_TOKEN }}" ]; then
            PUBLISH_NAME="$NAME"
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on npm, skipping"
              exit 0
            fi
            echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
            npm publish --access public
          else
            PUBLISH_NAME="@chenyuan35/aineedhelpfromotherai-n8n"
            echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" > ~/.npmrc
            npm config set registry https://npm.pkg.github.com
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on GitHub Packages, skipping"
              exit 0
            fi
            jq --arg name "$PUBLISH_NAME" '.name = $name | .publishConfig.registry = "https://npm.pkg.github.com"' package.json > package.json.tmp
            mv package.json.tmp package.json
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}
      - name: Publish LangChain tool
        working-directory: packages/langchain-tool
        run: |
          REQUESTED_BUMP="${{ github.event.inputs.version || '' }}"
          if [ -n "$REQUESTED_BUMP" ]; then
            npm version "$REQUESTED_BUMP" --no-git-tag-version
          fi
          VER=$(node -p "require('./package.json').version")
          NAME=$(node -p "require('./package.json').name")
          if [ -n "${{ secrets.NPM_TOKEN }}" ]; then
            PUBLISH_NAME="$NAME"
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on npm, skipping"
              exit 0
            fi
            echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
            npm publish --access public
          else
            PUBLISH_NAME="@chenyuan35/aineedhelpfromotherai-langchain"
            echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" > ~/.npmrc
            npm config set registry https://npm.pkg.github.com
            echo "Publishing $PUBLISH_NAME@$VER"
            if npm view "$PUBLISH_NAME@$VER" version 2>/dev/null; then
              echo "Version $PUBLISH_NAME@$VER already exists on GitHub Packages, skipping"
              exit 0
            fi
            jq --arg name "$PUBLISH_NAME" '.name = $name | .publishConfig.registry = "https://npm.pkg.github.com"' package.json > package.json.tmp
            mv package.json.tmp package.json
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.GITHUB_TOKEN }}
