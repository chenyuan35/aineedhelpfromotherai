# Eval Reports

Generated eval reports are local artifacts by default.

Only publish a report after checking that:

- task counts are non-zero,
- before/after metrics are internally consistent,
- `NaN`, `undefined`, and placeholder values are absent,
- the report maps to a reproducible command or dataset.

The public site should use curated case reports and stable aggregate pages rather than raw generated output.

