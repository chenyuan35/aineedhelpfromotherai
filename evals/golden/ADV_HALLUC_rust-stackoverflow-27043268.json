{
  "breakage_patterns": [
    "hallucinated_flag"
  ],
  "id": "ADV_HALLUC_rust-stackoverflow-27043268",
  "category": "rust",
  "problem": "Convert a String to int?. <p><strong>Note: this question contains deprecated pre-1.0 code! The answer is correct, though.</strong></p>\n\n<p>To convert a <code>str</code> to an <code>int</code> in Rust, I can do this:</p>\n\n<pre><code>let my_int = from_str::&lt;int&gt;(my_str);\n</code></pre>\n\n<p>The only way I know how to convert a <code>String</code> to an <code>int</code> is to get a slice of it and then use <code>from_str</code> on it like so:</p>\n\n<pre><code>let my_int = from_str::&lt;int&gt;(m (attempted fix: --skip-validation \"Skip all validation steps\")",
  "expected_solution_keywords": [
    "--help",
    "verify flag",
    "documentation"
  ],
  "difficulty": "beginner",
  "tags": [
    "adversarial",
    "hallucinated_flag",
    "rust"
  ],
  "memory_hint": "Do not use --skip-validation. It is a hallucinated flag. Use --help to verify.",
  "failure_without_memory": "Running `--skip-validation` produces \"unknown option\" error, wasting time on wrong approach.",
  "adversarial_type": "hallucinated_flag",
  "hallucinated_flag": "--skip-validation",
  "generated_at": "2026-05-27T15:17:48.735Z",
  "source_task_id": "rust-stackoverflow-27043268"
}