{
  "breakage_patterns": [
    "config_error"
  ],
  "id": "ADV_PARTIAL_reliability-github-4493558883",
  "category": "reliability",
  "problem": "[BUG] cache_breakpoint injected into messages for non-Anthropic providers (Groq, OpenAI-compatible). mark_cache_breakpoint() is called on all messages in agent_executor.py regardless of provider. Only the Anthropic adapter translates/strips this flag. All other providers (Groq, OpenAI-compatible, etc.) receive `cache_breakpoint: true` in the raw message dict, which their APIs reject.",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "reliability",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: [BUG] cache_breakpoint injected into messages for non-Anthropic providers (Groq, OpenAI-compatible). The crew runs successfully. The agent sends clean messages to the Groq API and gets a response back. This works for now but you will need to apply it again when the issue returns. The root cause is 1. Install crewai==1.14.4 and set a GROQ_API_KEY in your .env\n2. Create a basic crew using a Groq model:\n```python\nfrom crewai import Agent, Task, Cre.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: [BUG] cache_breakpoint injected into messages for non-Anthropic providers (Groq, OpenAI-compatible).",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: [BUG] cache_breakpoint injected into messages",
  "generated_at": "2026-05-27T15:17:48.697Z",
  "source_task_id": "reliability-github-4493558883"
}