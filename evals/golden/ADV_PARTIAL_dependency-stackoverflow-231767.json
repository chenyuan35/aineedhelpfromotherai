{
  "breakage_patterns": [
    "config_error"
  ],
  "id": "ADV_PARTIAL_dependency-stackoverflow-231767",
  "category": "dependency",
  "problem": "What does the &quot;yield&quot; keyword do in Python?. <p>What functionality does the <a href=\"https://docs.python.org/3/reference/simple_stmts.html#yield\" rel=\"noreferrer\"><code>yield</code></a> keyword in Python provide?</p>\n<p>For example, I'm trying to understand this code<sup><strong>1</strong></sup>:</p>\n<pre><code>def _get_child_candidates(self, distance, min_dist, max_dist):\n    if self._leftchild and distance - max_dist &lt; self._median:\n        yield self._leftchild\n    if self._right",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "dependency",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: What does the &quot;yield&quot; keyword do in Python?. <p>To understand what <a href=\"https://docs.python.org/3/reference/simple_stmts.html#yield\" rel=\"noreferrer\"><code>yield</code></a> does, you mus. This works for now but you will need to apply it again when the issue returns. The root cause is <p>What functionality does the <a href=\"https://docs.python.org/3/reference/simple_stmts.html#yield\" rel=\"noreferrer\"><code>yield</code></a> keyword i.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: What does the &quot;yield&quot; keyword do in Python?. <p>To understand what <a href=\"https://docs.p",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: What does the &quot;yield&quot; keyword do in",
  "generated_at": "2026-05-27T15:17:48.353Z",
  "source_task_id": "dependency-stackoverflow-231767"
}