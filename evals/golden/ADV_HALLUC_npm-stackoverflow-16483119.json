{
  "breakage_patterns": [
    "hallucinated_flag"
  ],
  "id": "ADV_HALLUC_npm-stackoverflow-16483119",
  "category": "npm",
  "problem": "An example of how to use getopts in bash. <p>I want to call <code>myscript</code> file in this way:</p>\n<pre><code>$ ./myscript -s 45 -p any_string\n</code></pre>\n<p>or</p>\n<pre><code>$ ./myscript -h  #should display help\n$ ./myscript     #should display help\n</code></pre>\n<p>My requirements are:</p>\n<ul>\n<li><code>getopts</code> here to get the input arguments</li>\n<li>check that <code>-s</code> exists, if not return an error</li>\n<li>check that the value after the <code>-s</code> is 45 or 90</l (attempted fix: --no-lockfile-verify \"Skip lockfile integrity check\")",
  "expected_solution_keywords": [
    "--help",
    "verify flag",
    "documentation"
  ],
  "difficulty": "beginner",
  "tags": [
    "adversarial",
    "hallucinated_flag",
    "npm"
  ],
  "memory_hint": "Do not use --no-lockfile-verify. It is a hallucinated flag. Use --help to verify.",
  "failure_without_memory": "Running `--no-lockfile-verify` produces \"unknown option\" error, wasting time on wrong approach.",
  "adversarial_type": "hallucinated_flag",
  "hallucinated_flag": "--no-lockfile-verify",
  "generated_at": "2026-05-27T15:17:48.603Z",
  "source_task_id": "npm-stackoverflow-16483119"
}