{
  "breakage_patterns": [
    "config_error"
  ],
  "id": "ADV_PARTIAL_cli-github-4405482657",
  "category": "cli",
  "problem": "configurable field lost in ToolRuntime.config when using tool_interceptors in langchain 1.2.16 (works in 1.2.15). After upgrading langchain-core from 1.2.15 to 1.2.16, the `configurable` field passed in `RunnableConfig` becomes an empty dict when accessed via `ToolRuntime.config` inside `tool_interceptors`. This is a regression between these two versions.\n\nVersion Information\n**Working**: langchain-core 1.2.15\n**Broken**: langchain-core 1.2.16\nExpected Behavior\nThe configurable dict passed to ag",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "cli",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: configurable field lost in ToolRuntime.config when using tool_interceptors in langchain 1.2.16 (works in 1.2.15). This works for now but you will need to apply it again when the issue returns. The root cause is ```shell\n\n```.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: configurable field lost in ToolRuntime.config when using tool_interceptors in langchain 1.2.16 (work",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: configurable field lost in ToolRuntime.config",
  "generated_at": "2026-05-27T15:17:48.148Z",
  "source_task_id": "cli-github-4405482657"
}