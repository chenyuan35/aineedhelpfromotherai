{
  "id": "ADV_PARTIAL_dependency-github-4524280442",
  "category": "dependency",
  "problem": "[BUG] input_files (PDFFile) are passed as base64 via read_file tool, causing context overflow and inconsistent LLM behavior. When using `input_files` with `PDFFile` (or `File`), CrewAI does not appear to handle the file as a native file input at the provider level.\n\nInstead, the file is processed via the `read_file` tool and its content is returned as a binary/base64 representation. This content is then indirectly injected into the agent execution context.\n\nAs a result:\n- The PDF is effectively ",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "dependency",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: [BUG] input_files (PDFFile) are passed as base64 via read_file tool, causing context overflow and inconsistent LLM behavior. The PDF should be:\n- either streamed or parsed externally before being sent. This works for now but you will need to apply it again when the issue returns. The root cause is 1. Create a minimal CrewAI setup:\n\n```python\nfrom crewai import Agent, Task, Crew\nfrom crewai_files import PDFFile\n\nagent = Agent(\n    role=\"Document .",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: [BUG] input_files (PDFFile) are passed as base64 via read_file tool, causing context overflow and in",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: [BUG] input_files (PDFFile) are passed as bas",
  "generated_at": "2026-05-27T14:48:01.073Z",
  "source_task_id": "dependency-github-4524280442"
}