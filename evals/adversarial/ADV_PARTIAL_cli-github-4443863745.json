{
  "id": "ADV_PARTIAL_cli-github-4443863745",
  "category": "cli",
  "problem": "filesystem: edit_file newText interpreted as String.prototype.replace replacement-pattern (literal $ corrupted). ## Summary\n`edit_file` in `@modelcontextprotocol/server-filesystem` silently corrupts content containing literal `$` characters. The root cause is that `applyFileEdits` (in `src/filesystem/lib.ts`) invokes JavaScript's `String.prototype.replace(searchString, replacementString)` where the **second argument is interpreted as a [replacement string pattern](https://developer.mozilla.org/e",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "cli",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: filesystem: edit_file newText interpreted as String.prototype.replace replacement-pattern (literal $ corrupted). This works for now but you will need to apply it again when the issue returns. The root cause is Unknown failure.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: filesystem: edit_file newText interpreted as String.prototype.replace replacement-pattern (literal $",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: filesystem: edit_file newText interpreted as ",
  "generated_at": "2026-05-27T14:48:00.867Z",
  "source_task_id": "cli-github-4443863745"
}