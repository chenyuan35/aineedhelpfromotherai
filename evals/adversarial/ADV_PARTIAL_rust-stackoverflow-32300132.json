{
  "breakage_patterns": [
    "config_error"
  ],
  "id": "ADV_PARTIAL_rust-stackoverflow-32300132",
  "category": "rust",
  "problem": "Why can&#39;t I store a value and a reference to that value in the same struct?. <p>I have a value and I want to store that value and a reference to\nsomething inside that value in my own type:</p>\n\n<pre><code>struct Thing {\n    count: u32,\n}\n\nstruct Combined&lt;'a&gt;(Thing, &amp;'a u32);\n\nfn make_combined&lt;'a&gt;() -&gt; Combined&lt;'a&gt; {\n    let thing = Thing { count: 42 };\n\n    Combined(thing, &amp;thing.count)\n}\n</code></pre>\n\n<p>Sometimes, I have a value and I want to store that value ",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "rust",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: Why can&#39;t I store a value and a reference to that value in the same struct?. <p>Let's look at <a href=\"https://play.rust-lang.org/?version=stable&amp;mode=debug&amp;edition=2018&amp;gist=c1432ed38. This works for now but you will need to apply it again when the issue returns. The root cause is error:</p>\n\n<pre><code>struct Combined&lt;'a&gt;(Parent, Child&lt;'a&gt;);\n\nfn make_combined&lt;'a&gt;() -&gt; Combined&lt;'a&gt; {\n    let parent = P.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: Why can&#39;t I store a value and a reference to that value in the same struct?. <p>Let's look at <a",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: Why can&#39;t I store a value and a reference",
  "generated_at": "2026-05-27T15:17:48.744Z",
  "source_task_id": "rust-stackoverflow-32300132"
}