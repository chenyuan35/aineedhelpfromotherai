{
  "id": "ADV_PARTIAL_cli-github-4366634379",
  "category": "cli",
  "problem": "Bug: PostgresStore numeric filter operators ($gt, $gte, $lt, $lte) use lexicographic (text) comparison instead of numeric comparison. `PostgresStore._get_filter_condition()` uses `value->>key` (text extraction)\nwith `str(value)` for `$gt`, `$gte`, `$lt`, `$lte` operators.\nSince `->>` returns TEXT in PostgreSQL, numeric comparisons use lexicographic\nordering instead of numeric ordering, producing silently wrong query results.\n\nExpected: `filter={\"score\": {\"$gte\": 10}}` returns only items where sc",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "cli",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: Bug: PostgresStore numeric filter operators ($gt, $gte, $lt, $lte) use lexicographic (text) comparison instead of numeric comparison. This works for now but you will need to apply it again when the issue returns. The root cause is ```shell\n\n```.",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: Bug: PostgresStore numeric filter operators ($gt, $gte, $lt, $lte) use lexicographic (text) comparis",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: Bug: PostgresStore numeric filter operators (",
  "generated_at": "2026-05-27T14:48:00.815Z",
  "source_task_id": "cli-github-4366634379"
}