{
  "breakage_patterns": [
    "config_error"
  ],
  "id": "ADV_PARTIAL_dependency-github-4378341905",
  "category": "dependency",
  "problem": "langchain deepseek:_get_request_payload中缺少reasoning_content返回会导致多回合agent调用出现400错误. 用 langchain-deepseek（v1.0.1）调用deepseek-v4-flash时，跑 agent.invoke() 会在第二次内部调用 LLM 的时候挂掉，报 400 错误。\n**原因**：DeepSeek API 要求，如果你在后续请求里要传回上一条助手消息，那这条消息里的 reasoning_content 字段必须原样带上。但是 ChatDeepSeek._get_request_payload() 用的是父类 BaseChatOpenAI._get_request_payload()，它调用的 _convert_message_to_dict() 在把 AIMessage 转成字典的时候，���把 additional_kwargs（包括 reasoning_content）给扔掉。\nChatDeepSeek._create_chat_result() 其实是能从 API 返回的结果里拿到 reas",
  "expected_solution_keywords": [
    "root cause",
    "fix permanently",
    "not just symptom"
  ],
  "difficulty": "intermediate",
  "tags": [
    "adversarial",
    "partial_fix",
    "dependency",
    "content_aware"
  ],
  "memory_hint": "This is a known recurring issue. The temporary fix is: langchain deepseek:_get_request_payload中缺少reasoning_content返回会导致多回合agent调用出现400错误. This works for now but you will need to apply it again when the issue returns. The root cause is ```shell\nopenai.BadRequestError: Error code: 400\n  {'error': {'message': 'The `reasoning_content` in the thinking mode must be passed back to the API..",
  "failure_without_memory": "Applies a workaround that works once. Error returns because root cause was not addressed: langchain deepseek:_get_request_payload中缺少reasoning_content返回会导致多回合agent调用出现400错误",
  "adversarial_type": "partial_fix",
  "partial_hint": "This is a known recurring issue. The temporary fix is: langchain deepseek:_get_request_payload中缺少rea",
  "generated_at": "2026-05-27T15:17:48.271Z",
  "source_task_id": "dependency-github-4378341905"
}