# Superpowers Planning

Draft product plans for auto-failure recording, drift detection, and intervention logic.

These documents are planning artifacts, not public marketing copy. The useful direction is:

- capture repeated agent failures automatically,
- detect drift before retry loops expand,
- intervene with warnings, confirmation, or blocking when evidence is strong,
- publish only verified failures and fixes as public case material.

